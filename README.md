# 207f

`207f` is a library for converting unicode characters to **readable** ASCII characters.

## Why

Latin characters have a lot of similar looking or sometimes identical characters in hight unicode ranges. For example:

> Ǳ  vs DZ

These characters are **not** the same but visually look similar, sometimes identical. These unicode characters can be abused to avoid moderation filters

## Usage

TODO
